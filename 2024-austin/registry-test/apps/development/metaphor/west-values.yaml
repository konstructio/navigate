metaphor:
  annotations:
    linkerd.io/inject: "enabled"
  labels:
    mirror.linkerd.io/exported: "true"
    config.linkerd.io/enable-debug-sidecar: "true"
  image:
    tag: 0.1.0-2024-navigate-austin
  ingress:
    className: nginx
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: 'letsencrypt-prod'
      nginx.ingress.kubernetes.io/service-upstream: "true"
    hosts:
      - host: metaphor-west.freegitopsmagic.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: metaphor-tls
        hosts:
          - metaphor-west.freegitopsmagic.com
  metaphor: # do we need these two urls?
    host: https://metaphor-west.freegitopsmagic.com/api
    console: https://kubefirst.freegitopsmagic.com
  
  useExternalSecret: false

  # clusterSecretStoreName: west-vault-kv-secret
  configs:
    configOne: west-cluster-override-one
    configTwo: west-cluster-override-two
